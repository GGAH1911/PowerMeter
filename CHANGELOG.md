# 변경 이력

이 파일은 사용자에게 보이는 변화만 기록합니다. 각 버전의 다운로드는 [Releases](https://github.com/GGAH1911/PowerMeter/releases)에 있습니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고, 버전은 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

## [1.8] — 2026-08-16

### 변경
- **온도 탭을 CPU와 배터리 두 값만 남기고 정리했습니다.** 정체를 알 수 없는 센서(`TVM4`·`TVm0`·`TCDX` 등)와 "가장 뜨거운 센서" 목록을 없앴습니다. 키 이름만으로는 무엇을 재는지 알 수 없는 숫자를 나열하면, 읽는 사람이 없는 의미를 읽어내게 됩니다. 남긴 둘은 정체가 확인된 것들입니다 — CPU는 계열이 확인된 `Tp*`/`Te*`/`Tf*`의 최댓값, 배터리는 IOKit과 교차검증된 `TB*`입니다.
- **그 자리에 온도 그래프가 들어갑니다.** CPU와 배터리를 **지난 1시간** 동안 함께 보여줍니다. 두 선은 같은 축을 씁니다 — 축을 따로 주면 배터리의 2°C 흔들림이 큰 추세처럼 보이기 때문입니다.

기록은 표본 개수가 아니라 시각으로 잘라내므로, 갱신 주기를 1초로 하든 5초로 하든 창은 1시간으로 유지됩니다. 메모리에만 두고 디스크에는 쓰지 않습니다.

## [1.7.2] — 2026-08-16

### 수정
- **M3에서 CPU 온도를 과소 표시하던 문제.** `Tf*` 계열을 CPU에서 빼먹었습니다. M3은 `Te*`가 효율 코어, `Tf*`가 성능 코어라, 더 뜨거운 성능 코어를 무시하고 효율 코어 온도를 CPU 온도로 보고했을 것입니다.
- **시작 직후 약 1분간 상수 센서가 최고 온도로 표시되던 문제.** 상수 판정에 두 번의 스윕(60초)이 필요해서, M2 Max에서 첫 1분간 `Tf46` 102.8°C가 빨갛게 떴습니다. 이제 시작 시 5초 간격으로 두 번 표본을 떠서 바로 판정합니다. 실측으로 8초 시점부터 정상값(53.1°C)이 나옵니다.
- **한 번 상수로 판정되면 영원히 배제되던 문제.** 값이 다시 움직이면 판정이 해제됩니다. 잠시 멈춰 있었을 뿐인 센서를 영구히 잃지 않습니다.

`Tf*`를 CPU에 포함하면서 상수 필터의 면제 범위를 배터리 하나로 좁혔습니다. 같은 접두사가 칩마다 다른 것을 가리키기 때문입니다 — `Tf`는 M3에서 성능 코어이지만 M2 Max에서는 102.8°C 고정 상수입니다. 계열 이름은 표시용 라벨일 뿐이고, 살아있는 센서인지는 실제 값의 움직임으로만 판단합니다. 배터리만 예외인 이유는 IOKit과 교차검증됐고 정지 상태에서 값이 고정되는 것이 정상이기 때문입니다.

### 검증
소유하지 않은 모델은 실기 검증이 불가능하므로, 커뮤니티가 문서화한 칩별 키 집합(M1·M2·M3·M4/M5)을 분류기에 통과시켜 확인했습니다. 여섯 개 칩 모두에서 CPU와 배터리 센서를 찾습니다. 실기 검증은 M1 맥북에어와 M2 Max 맥북프로 두 대에서 했습니다.

## [1.7.1] — 2026-08-16

### 수정
- **온도가 아닌 값이 "최고 온도"로 표시되던 문제.** 일부 SMC 키는 실제 측정값이 아니라 고정 상수입니다. M2 Max의 `Tf46`은 유휴·전부하·냉각 후 모두 **102.8°C로 미동도 없습니다.** 앱은 이걸 최고 온도로 빨갛게 상시 표시했습니다. 이제 계열을 식별할 수 없으면서 두 번의 스윕에 걸쳐 값이 전혀 변하지 않은 센서는 순위에서 제외합니다. 실측 결과 최고 온도가 102.8°C에서 74.1°C로 정상화됐습니다.
- **전원이 내려간 CPU 코어의 쓰레기값이 표시되던 문제.** 부하가 끝나 코어가 꺼지면 8.4°C 같은 값을 냅니다. 스윕에만 있던 타당범위 필터(15~110°C)를 매 틱 경로에도 적용했습니다.

배터리처럼 정지 상태에서 값이 안 변하는 게 정상인 센서는 제외 대상이 아닙니다. 계열이 식별된 키(`TB*`·`Tp*`·`Te*` 등)는 면제하고, 정체를 알 수 없는 키에만 적용합니다.

### 검증
맥북프로(M2 Max, macOS 26.5.2)에서 실제 확인했습니다 — **팬 2개 인식**, 배터리 `TB0T` 유지, CPU `Tp0A` 정상.

SMC 키 체계는 macOS 버전이 아니라 **칩에 따라 다릅니다.** 같은 macOS 26.5.2에서 M2 Max는 커뮤니티가 문서화한 `Tp01`·`Tp05`·`Tp09`를 갖고 있지만, M1 맥북에어는 그중 하나도 없고 `Tp2z`~`Tp9z` 형태의 다른 체계를 씁니다. 개별 센서에 이름을 붙이지 않는 방침이 옳았음을 확인했습니다.

## [1.7] — 2026-08-16

### 추가
- **CPU 온도.** SMC의 `Tp*`(CPU 코어)·`Te*`(CPU 다이) 센서 중 최댓값을 CPU 온도로 표시합니다. 온도 탭 맨 위에 오고, 흐름 탭의 맥 노드에도 붙습니다. 매 틱 실제 최댓값을 다시 고르므로 코어 간 순위가 바뀌어도 따라갑니다.
- **센서 계열 이름.** `Tp*` CPU 코어, `Te*` CPU 다이, `Th*` SoC 히트싱크, `Ts*` SSD, `TB*` 배터리, `Tg*` GPU. 계열까지만 표기하고 개별 센서에는 이름을 붙이지 않습니다 — 같은 키가 칩 세대마다 다른 코어를 가리키기 때문입니다.
- CPU 계열이 아닌 센서가 더 뜨거우면 "최고 온도"를 따로 한 줄 보여줍니다.

### 수정
- **흐름 탭의 맥 노드가 배터리 온도를 보여주던 문제.** 맥의 소비 전력을 표시하는 칸에 배터리 온도가 붙어 있었습니다. 이제 맥 노드는 CPU 온도를, 배터리 노드는 배터리 온도를 각각 표시합니다.

계열 구분은 값 분포로 뒷받침됩니다. 이 기기에서 CPU 계열(`Tp`·`Te`, 39~62°C)이 가장 뜨겁고, SSD(`Ts`)가 중간, 배터리(`TB`, 31~33°C)가 가장 낮으며 `TB*`는 IOKit이 보고하는 배터리 온도와 일치합니다. `TC*`는 같은 계열 안에서 62°C와 38°C가 섞여 있어 CPU 집계에서 제외했습니다 — `TCMz`가 가장 뜨거운 `Tp` 센서와 값이 같아 빼도 최댓값은 변하지 않습니다.

## [1.6.2] — 2026-08-16

### 수정
- **배터리 온도가 탭마다 다르게 보이던 문제.** 흐름·건강 탭은 IOKit의 `Temperature`(30.8°C)를, 온도 탭은 SMC `TB*` 센서 평균(34.0°C)을 쓰고 있었습니다. 같은 이름의 값이 화면마다 4°C씩 달랐습니다. 이제 세 탭 모두 하나의 값을 씁니다.

### 변경
- **배터리 온도의 출처를 SMC로 통일했습니다.** SMC의 `TB0T`는 IOKit의 `VirtualTemperature`와 0.01°C 차이로 같은 측정값이며, 60초를 기다리지 않고 매 틱 갱신됩니다. SMC를 열 수 없는 환경에서는 기존 IOKit 값으로 폴백합니다.
- 평균 대신 `TB0T` 하나를 씁니다. `TB2T`가 다른 센서보다 2~3°C 낮아, 평균을 내면 **어떤 출처도 보고하지 않는 숫자**가 만들어집니다.
- 온도 탭이 배터리 온도 옆에 출처 키(`TB0T`)를 표시합니다.

IOKit은 배터리 온도를 `Temperature`(30.8°C)와 `VirtualTemperature`(34.9°C) 두 개로 갖고 있고 서로 4°C 어긋납니다. 둘 다 정상이며 팩의 다른 지점을 잽니다. 어느 쪽이 유일한 진실이라고 부를 수 없어, 측정을 더 빨리 주는 쪽을 골랐습니다.

## [1.6.1] — 2026-08-16

### 수정
- **온도 경고색이 빨간색으로 절대 바뀌지 않던 문제.** 조건이 `80 이상이면 주황, 95 이상이면 빨강` 순서로 쓰여 있어, 95°C 이상도 이미 80°C 조건에 걸려 주황으로 표시됐습니다. 빨간 분기는 도달할 수 없는 코드였습니다.

### 변경
- **온도 경고 기준을 올렸습니다** (주황 80 → 90°C, 빨강 95 → 100°C). Apple Silicon의 다이 센서는 부하 중 80~100°C가 정상 동작 범위이고 그 위에서 스로틀링이 걸립니다. 기존 기준은 평범한 작업을 이상 상태로 표시했습니다.
- 온도 탭 설명을 보강했습니다. 같은 SMC 키가 칩 세대에 따라 다른 센서를 가리키기도 한다는 점과, 가장 뜨거운 센서는 보통 다이 온도라 높게 나오는 것이 정상이라는 점을 명시합니다.

## [1.6] — 2026-08-16

### 추가
- **온도 탭.** SMC의 온도 센서를 실시간으로 읽습니다. 최고 온도와 그 센서, 배터리 온도, 팬 회전수, 그리고 가장 뜨거운 센서 6개를 보여줍니다. 전력을 많이 쓰면 뜨거워지고 뜨거워지면 스로틀링이 걸리므로, 전력과 같은 화면에서 볼 값입니다.
- **전력 추이 그래프.** 흐름 탭 아래에 시스템 소비 스파크라인이 붙었습니다. 순간값만 보이던 것에서 "방금 뭘 했더니 튀었다"를 볼 수 있게 됐습니다. 최근 300샘플을 메모리에만 보관하며 디스크에 남기지 않습니다.
- **오늘 사용 구간.** 하루 동안 배터리가 오간 SoC 범위를 건강 탭에 표시합니다. 충전 제한이 실제로 작동하는지 한 줄로 확인됩니다.
- **배터리 평생 기록.** 총 가동 시간, 겪은 온도 범위, 최대 충·방전 전류, 팩 전압 범위. 영구고장·셀단선 지표는 0이 아닐 때만 나타납니다.

### 변경
- SMC 키의 크기·타입을 캐싱합니다. 키 하나 읽는 비용이 0.40ms에서 **0.12ms로 3.3배** 빨라졌습니다.
- 탭이 5개(흐름·온도·건강·충전·설정)가 되면서 팝오버 높이가 340pt에서 364pt로 늘었습니다.

온도 센서는 146개가 있지만 전부 읽으면 틱당 28ms가 들어 전력 모니터가 전력을 쓰게 됩니다. 60초마다 전체를 훑어 가장 뜨거운 6개를 고르고, 매 틱에는 그 6개만 읽습니다(1.65ms). 전체 훑기는 백그라운드에서 돌아 UI를 막지 않습니다.

센서 키 이름(`TCMz`, `Tp4z` 등)은 Apple이 공개하지 않아 원문 그대로 표시합니다. 예외는 `TB*`로, IOKit이 보고하는 배터리 온도와 값이 일치해 배터리 센서로 확인됐습니다.

## [1.5.3] — 2026-08-16

### 변경
- **메뉴바 표시 미리보기 행을 단순화했습니다.** "적용 안 됨" 경고와 "되돌리기" 버튼을 없앴습니다. 확인 버튼이 떠 있다는 것 자체가 아직 적용되지 않았다는 뜻이고, 다른 항목을 다시 고르면 되돌아가므로 같은 말을 세 번 하고 있었습니다.

### 수정
- **설정 탭 하단 버전 문자열에 가로선이 그어져 보이던 문제.** 설정 탭 내용이 263pt인데 탭에 주어진 높이가 250pt여서 13pt가 넘쳤고, 맨 아래 버전 문자열이 그 아래 구분선 위로 겹쳐 그려졌습니다. 탭 높이를 268pt로 넓혀 가장 긴 탭이 여유 있게 들어갑니다.

## [1.5.2] — 2026-08-16

### 수정
- **팝오버를 열고 잠시 후 상단 탭 바(흐름·건강·충전·설정)가 사라지던 문제.** 팝오버에 선언된 크기가 380×380이었는데 실제 내용 높이는 380×322로, 58pt가 어긋나 있었습니다. `NSPopover`는 화면에 뜬 뒤 내용의 실제 크기를 따라가며 줄어드는데, 그 차이를 위쪽에서 잘라내면서 탭 바가 있는 영역이 통째로 사라졌습니다. 이제 크기를 손으로 적지 않고 뷰 자신의 크기에서 가져오므로 어긋날 수 없습니다.

기기에 따라 증상이 다르게 보였습니다(맥북에어는 정상, 맥북프로는 재현). 리사이즈 타이밍 차이일 뿐 크기 불일치는 두 기기 모두에 있었습니다.

## [1.5.1] — 2026-08-16

### 수정
- **SMC 키가 일부만 존재할 때 어댑터 전력을 0으로 잘못 보고하던 문제.** 시스템 전력(`PSTR`)은 읽히는데 어댑터 전력(`PDTR`)이 없는 기기에서, 어댑터 전력이 조용히 0이 되어 실제로는 충전 중인데도 "어댑터 유휴"로 표시될 수 있었습니다. 이제 두 키가 모두 읽혀야만 SMC 값을 쓰고, 하나라도 없으면 IOKit 값으로 폴백합니다. 낡은 값을 보여주는 것이 틀린 값을 확신 있게 보여주는 것보다 낫기 때문입니다.

현재 하드웨어에서는 발현되지 않는 잠재 버그입니다. SMC 인터페이스에서 실제로 변하는 부분이 프로토콜이 아니라 키 이름이라, 새 칩 세대에서 의미가 있습니다.

## [1.5] — 2026-08-16

### 추가
- **어댑터 효율 손실.** 어댑터가 벽에서 끌어오는 전력과 맥에 실제 도달하는 전력의 차이를 표시합니다. 건강 탭에 손실 W·효율%·벽면 소비 추정치가 나옵니다. 90% 미만이면 주황색으로 표시합니다.
- **USB-PD 협상 프로파일.** 어댑터가 제공하는 전압/전류 조합 전체와 그중 실제로 협상된 것을 표시합니다. "30W 어댑터인데 왜 12V 2.5A인가" 같은 의문을 바로 확인할 수 있습니다.
- **충전이 보류된 이유.** 어댑터가 연결됐는데 충전이 안 될 때 충전 탭에 사유를 표시합니다 — 충전 상한 도달, 완충, 온도 제한, 충전기 억제 순으로 확인합니다.
- **셀 단위 정보.** 3셀 각각의 전압, 셀 간 편차, 내부 저항을 표시합니다. 편차 50mV 이상이면 주황색입니다.

셀 편차는 부하가 걸리면 커집니다(실측: 무부하 1mV → 방전 중 25mV). 저항이 가장 높은 셀이 가장 많이 떨어지기 때문이며, 앱에도 이 설명을 함께 표시합니다.

`NotChargingReason`은 비트 7(어댑터 미연결)만 실측으로 확인했습니다. 나머지 비트는 의미를 단정하지 않고, 앱이 이미 아는 상태(상한 도달·완충·온도)를 우선 사용하며 해독되지 않은 비트는 원시 16진값으로 표시합니다.

## [1.4] — 2026-08-16

### 변경
- **전력값을 SMC에서 실시간으로 읽습니다.** 지금까지 쓰던 IOKit `AppleSmartBattery`는 **60초에 한 번만** 값을 갱신합니다 — `UpdateTime`이 정확히 60초 간격으로만 움직이고 그사이 모든 필드가 바이트 단위로 동일합니다. 즉 1초로 설정해도 같은 값을 59번 다시 읽을 뿐이었습니다. 이제 시스템 소비와 어댑터 공급은 SMC(`PSTR`/`PDTR`)에서 직접 읽어 매 틱 실제로 갱신되고, 배터리 흐름은 둘의 차이로 구합니다. **갱신 주기 설정이 비로소 실제 효과를 갖습니다.**
- **IOKit 폴링을 60초로 낮췄습니다.** 용량·사이클·건강·어댑터 사양처럼 원래 느리게 변하는 값만 IOKit에서 읽습니다. 어댑터를 꽂거나 뽑으면 이벤트로 즉시 다시 읽으므로 반응성은 그대로입니다. 1초 설정 기준 분당 IOKit 조회가 60회에서 1회로 줄어, 전력 모니터가 전력을 덜 쓰게 됐습니다.
- 설정 탭에 데이터 출처를 표시합니다. SMC를 못 여는 환경에서는 기존 IOKit 값으로 자동 폴백하고 그 사실을 알립니다.

SMC 접근에는 관리자 권한이 필요 없으며, battery 엔진 설치 여부와도 무관합니다.

## [1.3] — 2026-08-15

### 변경
- **메뉴바 표시 모드가 확인을 눌러야 적용됩니다.** 이전에는 선택하는 즉시 반영돼서 미리보기를 볼 시점엔 이미 메뉴바가 바뀐 뒤였습니다. 이제 선택은 초안만 바꾸고, 미리보기에 "적용 안 됨" 표시와 함께 **확인** / **되돌리기** 버튼이 나타납니다. 팝오버를 닫으면 확정되지 않은 초안은 버려집니다.

### 수정
- **설정 탭에서 선택하지 않은 항목이 보이지 않던 문제.** 세그먼트 피커가 어두운 배경에서 비선택 항목을 거의 렌더링하지 않아 어떤 선택지가 있는지 알 수 없었습니다. 충전 탭에서 쓰던 칩 UI로 통일하고 테두리와 밝은 라벨을 적용했습니다. 갱신 주기 선택에도 동일하게 반영됩니다.

## [1.2] — 2026-08-15

### 추가
- **충전 탭에서 엔진 설치·제거.** 관리자 인증 한 번으로 끝나며 터미널이 필요 없습니다. 승인 전에 실행될 명령과 건드릴 경로(`/usr/local/co.palokaj.battery/`, `/etc/sudoers.d/battery`, `/etc/paths.d/50-battery`)를 그대로 보여줍니다.
- **충돌 앱 감지.** AlDente 또는 battery.app이 설치돼 있으면 설치 전에 경고합니다. 두 앱이 같은 SMC 충전 키를 두고 충돌합니다.

### 수정
- **제거 후 root 소유 잔여물이 남던 문제.** `battery uninstall`에 위임해 sudoers 규칙·백그라운드 데몬·바이너리·설정까지 정리하고 충전 제한을 해제합니다. 참고로 `brew uninstall --cask battery`는 `/usr/local/bin/smc` 심볼릭 링크만 지우고 sudoers 규칙과 데몬은 남깁니다.

## [1.1] — 2026-08-15

### 추가
- **메뉴바 표시 폭 선택.** 충전 중 상태바가 `🔌 29% 8.4W ＋18.3W`(17자)까지 길어지던 문제를 해결했습니다. 아이콘(1자) / 잔량(5자) / 전력(6자) / 전체 네 단계 중 고를 수 있고 미리보기가 함께 표시됩니다. 색상 규칙과 즉시 반응하는 🔌/🔋 아이콘은 모든 모드에서 유지됩니다.
- **Homebrew 설치.** `brew install --cask ggah1911/tap/powermeter`

## [1.0] — 2026-08-15

첫 공개 릴리스.

### 추가
- **메뉴바 실시간 전력** — 시스템 소비 / 어댑터 공급 / 배터리 충·방전 W + 배터리 %
- **전력 흐름 다이어그램** — 충전 · 동시 사용 · 어댑터 구동 · 배터리 구동 4상태 자동 판별과 애니메이션
- **배터리 건강 대시보드** — 건강%, 설계·최대 용량, 사이클, 온도, 전압, 어댑터 사양, 에너지 사용 상위 앱
- **충전 제어** — 충전 상한 슬라이더·프리셋, Sailing(범위 유지), 강제 방전/충전, 캘리브레이션
- **설정** — 갱신 주기, 소수점 표시, 로그인 시 자동 시작
- 흐름 탭의 맥 노드에 배터리 온도 표시 (35°C 이상 주황색)
- 어댑터 연결·해제 시 즉시 반응하는 메뉴바 아이콘

[1.8]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.8
[1.7.2]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.7.2
[1.7.1]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.7.1
[1.7]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.7
[1.6.2]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.6.2
[1.6.1]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.6.1
[1.6]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.6
[1.5.3]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.5.3
[1.5.2]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.5.2
[1.5.1]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.5.1
[1.5]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.5
[1.4]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.4
[1.3]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.3
[1.2]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.2
[1.1]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.1
[1.0]: https://github.com/GGAH1911/PowerMeter/releases/tag/v1.0
